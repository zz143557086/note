```
获取
element.属性 获取属性值 元素只带的
element.getAttribute('属性') 可以获取程序员赋予的属性
eg 属性data_list_name
dataset存放所有以data开始的属性
element.dataset.listName
更改
element.属性='值'
element.setAttribute('属性','值')主要针对自定义
移除
element.removeAttribute('属性')针对自定义的
```

